<%
files = {}
Dir.chdir("#{Rails.root}/app/assets/") do
  Dir['images/**/*'].each do |path|
    file = path.dup
    path.slice! 'images/'
    files[file] = image_path(path)
  end
end
%>

window.asset_path = (name) ->
  <%= files.to_json %>[name]
